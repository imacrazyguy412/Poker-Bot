<configuration scan="true" scanPeriod="1 seconds" debug="false">
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{HH:mm:ss.SSS} [%thread] %level %logger{0} - %msg%n%ex
            </pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--<file>logs.log</file>-->

        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>logs/%d{yyyy-MM-dd}.log</fileNamePattern>

            <maxHistory>1</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %level %logger{-1} - %msg%n%ex</pattern>
        </encoder>
    </appender> 

    <!--The program replaces the string "___REPLACE_LOG_LEVEL___" with the appropriate log level the loggers should log at-->
    <root level="___REPLACE_LOG_LEVEL___">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>